<Window x:Class="EasySave.GUI.Views.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:EasySave.GUI.ViewModels"
        Title="Paramètres - EasySave" Height="600" Width="450"
        Background="#F5F5F5" WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Paramètres" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
            <StackPanel>

                <GroupBox Header="Général" Margin="0,0,0,15" Padding="10" BorderBrush="#CCCCCC">
                    <StackPanel>
                        <TextBlock Text="Langue de l'interface :" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox SelectedIndex="0" Margin="0,0,0,15">
                            <ComboBoxItem Content="Français (FR)"/>
                            <ComboBoxItem Content="English (US)"/>
                        </ComboBox>

                        <TextBlock Text="Format des fichiers (Logs/State) :" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <RadioButton Content="JSON" IsChecked="{Binding IsJsonFormat}" Margin="0,0,20,0"/>
                            <RadioButton Content="XML" IsChecked="{Binding IsXmlFormat}"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Logiciels Métier (Interruption)" Margin="0,0,0,15" Padding="10" BorderBrush="#CCCCCC">
                    <StackPanel>
                        <TextBlock Text="Ajouter un exécutable (ex: calculatrice.exe) :" Margin="0,0,0,5"/>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding NewBusinessSoftware, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0" Padding="2"/>
                            <Button Grid.Column="1" Content="Ajouter" Command="{Binding AddBusinessSoftwareCommand}" Padding="15,2"/>
                        </Grid>

                        <ListBox ItemsSource="{Binding BusinessSoftwares}" SelectedItem="{Binding SelectedBusinessSoftware}" Height="80" Margin="0,0,0,5"/>
                        <Button Content="Supprimer la sélection" Command="{Binding RemoveBusinessSoftwareCommand}" HorizontalAlignment="Right" Padding="10,2"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Extensions à crypter (CryptoSoft)" Margin="0,0,0,10" Padding="10" BorderBrush="#CCCCCC">
                    <StackPanel>
                        <TextBlock Text="Saisir une extension (ex: .txt) :" Margin="0,0,0,5"/>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding NewCryptoExtension, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0" Padding="2"/>
                            <Button Grid.Column="1" Content="Ajouter" Command="{Binding AddCryptoExtensionCommand}" Padding="15,2"/>
                        </Grid>

                        <ListBox ItemsSource="{Binding CryptoExtensions}" SelectedItem="{Binding SelectedCryptoExtension}" Height="80" Margin="0,0,0,5"/>
                        <Button Content="Supprimer la sélection" Command="{Binding RemoveCryptoExtensionCommand}" HorizontalAlignment="Right" Padding="10,2"/>
                    </StackPanel>
                </GroupBox>

            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Fermer" Click="CloseWindow_Click" Padding="15,8" Margin="0,0,10,0" Background="#DDDDDD"/>
            <Button Content="Enregistrer" Command="{Binding SaveOptionsCommand}" Background="#007ACC" Foreground="White" Padding="15,8" FontWeight="SemiBold">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="3"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>