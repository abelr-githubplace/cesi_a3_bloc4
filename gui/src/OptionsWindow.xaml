<Window x:Class="EasySave.GUI.Views.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lang="clr-namespace:EasySave.lang"
        Title="{x:Static lang:Messages.OptionMenuTitle}" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="{x:Static lang:Messages.ChooseLanguage}"/>
        <ComboBox Grid.Row="0" Grid.Column="1" SelectedValue="{Binding language}" SelectedValuePath="Content" Margin="0,0,0,10">
            <ComboBoxItem Content="FR"/>
            <ComboBoxItem Content="EN"/>
        </ComboBox>

        <Label Grid.Row="1" Grid.Column="0" Content="{x:Static lang:Messages.LogFormat}"/>
        <ComboBox Grid.Row="1" Grid.Column="1" SelectedValue="{Binding LogFormat}" SelectedValuePath="Content" Margin="0,0,0,10">
            <ComboBoxItem Content="JSON"/>
            <ComboBoxItem Content="XML"/>
            <ComboBoxItem Content="TXT"/>
        </ComboBox>

        <Label Grid.Row="3" Grid.Column="0" Content="{x:Static lang:Messages.BuisinessSoftware}"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding BusinessSoftwareName}" ToolTip="Ex Calculator" VerticalAlignment="Center"/>

        <Label Grid.Row="2" Grid.Column="0" Content="{x:Static lang:Messages.ExtensionsToEncrypt}"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding ExtensionsToEncrypt}" ToolTip="Ex: .txt, .pdf, .docx" Margin="0,3,0,0" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Fermer" Click="Close_Click" Width="75"/>
        </StackPanel>
    </Grid>
</Window>