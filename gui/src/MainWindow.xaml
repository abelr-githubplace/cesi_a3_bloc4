<Window x:Class="EasySave.GUI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="EasySave 2.0" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="➕ Nouveau Travail" Command="{Binding AddJobCommand}" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="⚙️ Options" Command="{Binding OpenOptionsCommand}" Margin="0,0,10,0" Padding="10,5"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding SaveJobs}" SelectedItem="{Binding SelectedJob}" 
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Source" Binding="{Binding SourcePath}" Width="2*"/>
                <DataGridTextColumn Header="Cible" Binding="{Binding TargetPath}" Width="2*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*"/>
                <DataGridTextColumn Header="État" Binding="{Binding State}" Width="*"/>
                <DataGridTextColumn Header="Progression" Binding="{Binding Progress, StringFormat={}{0}%}" Width="*"/>
            </DataGrid.Columns>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Modifier" Command="{Binding EditJobCommand}" CommandParameter="{Binding SelectedJob}"/>
                    <MenuItem Header="Supprimer" Command="{Binding DeleteJobCommand}" CommandParameter="{Binding SelectedJob}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="▶ Exécuter Sélectionné" Command="{Binding RunSelectedJobCommand}" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="▶▶ Exécuter Tout" Command="{Binding RunAllJobsCommand}" Padding="10,5"/>
        </StackPanel>
    </Grid>
</Window>